import React from 'react'

const PlayerScore = ({ scores } :Props) => {
    return (
        <div>
            0
        </div>
    )
}

interface Props {
    scores: {};
}

export default PlayerScore